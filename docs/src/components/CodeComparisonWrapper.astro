---
import CodeComparison from './CodeComparison';

interface Props {
  rust: string;
  swift: string;
  kotlin: string;
  java?: string;
  csharp?: string;
  typescript?: string;
  title?: string;
}

const { rust, swift, kotlin, java, csharp, typescript, title } = Astro.props;
---

<CodeComparison
  rust={rust}
  swift={swift}
  kotlin={kotlin}
  java={java}
  csharp={csharp}
  typescript={typescript}
  title={title}
  client:load
/>
