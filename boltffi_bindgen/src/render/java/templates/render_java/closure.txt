package {{ package_name }};

@FunctionalInterface
public interface {{ closure.interface_name }} {
{%- match closure.return_info %}
{%- when Some with (ret) %}
    {{ ret.java_type() }} invoke({% for param in closure.params %}{{ param.java_type }} {{ param.name }}{% if !loop.last %}, {% endif %}{% endfor %});
{%- when None %}
    void invoke({% for param in closure.params %}{{ param.java_type }} {{ param.name }}{% if !loop.last %}, {% endif %}{% endfor %});
{%- endmatch %}
}
